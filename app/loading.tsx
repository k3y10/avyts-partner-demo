import { LoadingState } from "@/components/states";

export default function Loading() {
  return <LoadingState title="Loading terrain intelligence" description="Syncing Wasatch Front conditions and terrain overlays." />;
}
